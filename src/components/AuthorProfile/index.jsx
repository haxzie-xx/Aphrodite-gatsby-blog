import React from 'react';
import { StaticQuery, graphql } from 'gatsby'

const AuthorProfile = ({}) => {

    return (
        <StaticQuery
            query={
                graphql``
            }/>
    );
}